import Router from "express"
import {login , signup} from '../controller/auth.controller.<%= ext %>'
const router = Router();

router.route("/login").post(login);
router.route("/signup").post(signup);